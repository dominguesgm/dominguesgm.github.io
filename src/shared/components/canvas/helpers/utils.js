function generateVectors(number, mouse) {
	const translationVectors = [];
	const { mouseX, mouseY } = mouse;
	const width = window.innerWidth;
	const height = window.innerHeight;

	for(let i = 0; i < number; i++) {

	}
}

export {
	generateVectors,
};
